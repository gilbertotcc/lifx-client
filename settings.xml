<settings>
    <servers>
        <server>
            <id>${env.BINTRAY_ID}</id>
            <username>${env.BINTRAY_USERNAME}</username>
            <password>${env.BINTRAY_API_KEY}</password>
        </server>
    </servers>
</settings>
